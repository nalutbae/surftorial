{
  "buildCommand": "cd tech && pnpm install --frozen-lockfile && pnpm build",
  "outputDirectory": "tech/.next",
  "installCommand": "cd tech && pnpm install --frozen-lockfile",
  "framework": "nextjs"
}
