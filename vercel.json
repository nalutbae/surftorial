{
  "buildCommand": "npm install && cd tech && npm install && npx prisma generate && npx next build",
  "outputDirectory": "tech/.next",
  "installCommand": "npm install && cd tech && npm install",
  "framework": "nextjs"
}
