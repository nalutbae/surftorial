{
  "buildCommand": "cd tech && pnpm install && pnpm exec prisma generate && pnpm build",
  "outputDirectory": "tech/.next",
  "installCommand": "cd tech && corepack prepare pnpm@11.7.0 --activate && pnpm install",
  "framework": "nextjs"
}
